import {headers} from './fakedata/headdata';
import fakerows from './fakedata/rowdata';

export const data = {
    fake: headers.headCellsFake,
    products: headers.headCellsProducts,
    customers: headers.headCellsCustomers,
    rowdata: fakerows,
}